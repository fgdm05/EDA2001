#ifndef Q1_DECLARATIONS
#define Q1_DECLARATIONS
#include "def_binary_tree.h"

    Arvore* inicia_q1();
    void pre_order(No* no);
    void in_order(No* no);
    void pos_order(No* no);
    void q1();

#endif